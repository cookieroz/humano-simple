'use strict';

/**
 * @ngdoc function
 * @name humanoApp.controller:VideoCtrl
 * @description
 * # VideoCtrl
 * Controller of the humanoApp
 */
angular.module('humanoApp')
  .controller('VideoCtrl', function ($scope) {
    $scope.video = {
      id: 'seqzSKbxgME'
    };
    // create a mute and unmute button
    // test on mobile
});
